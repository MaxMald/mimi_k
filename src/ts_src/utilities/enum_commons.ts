export enum Direction4 
{
    kUp,
    kDown,
    kRigh,
    kLeft
}

export enum Axis
{
    kHorizontal,
    kVertical
}

export enum PositionID
{
    kTop,
    kBottom,
    kLeft,
    kRight
}

export enum Rotation
{
    kCW,
    kCCW
}